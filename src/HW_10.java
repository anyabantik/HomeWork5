import java.io.FileInputStream;
import java.io.Serializable;

public class HW_10 implements Serializable {
    //Десериализовать объекты из задания #9. Отсортировать их по объёму.
    //Вывести на экран. Записать в файл (OUTPUT) самый большой объем.

  //  FileInputStream fiStream = new FileInputStream("box.dat");
  //  ObjectInputStream objectStream = new ObjectInputStream(fiStream);
  //  Object object = objectStream.readObject();
   //     fiStream.close();
    //    objectStream.close();


}
